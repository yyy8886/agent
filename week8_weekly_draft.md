# 本周工作内容（2026.08.10 - 08.14）

## 一、My Agent Next 稳定版本完成

本周集中完成了 `My Agent Next` 的功能补齐、架构整理和真实运行验证。项目从前期只能配置模型、创建 Agent 和进行基础对话，推进到具备 Skill 管理、环境识别、持久化索引、兼容性扫描、流式对话、用户记忆以及原生 LangGraph 工作流执行能力的稳定版本。

本周开发过程中坚持先定位问题、再补回归测试的方式，对梅贝尔的实际调用环境、Skill 加载位置、Agent 与 Skill 绑定、页面切换导致回答丢失、流式输出不生效、市场下载失败、工作流无法运行等问题逐项进行了排查。当前主要功能已经形成闭环，完整测试达到 60 项并全部通过，两个实际工作流也完成了端到端运行验证。

## 二、运行环境识别与跨平台兼容

针对梅贝尔在 Windows 主机上误判为 WSL/Linux 环境的问题，重新梳理了“宿主系统”和“命令实际运行平台”的区别。明确项目当前开发环境是 Windows PowerShell，未来部署目标为 Linux，不能依赖 WSL 作为中间环境，也不能在 Skill 中写死某一台机器的绝对路径。

本周对项目路径进行了相对路径改造，Python 模块、数据库、Skill、脚本和静态资源均根据当前文件位置解析。环境检查和系统时间等 Skill 分别提供 PowerShell 与 Bash 入口，使 Agent 能根据当前平台选择 Windows 或 Linux 原生命令。项目 Python 版本范围也进行了显式约束，为后续 Linux 部署和虚拟环境重建提供了统一依据。

同时清理了早期测试脚本和无用临时文件，将验证逻辑迁移为可重复执行的单元测试，减少项目目录中的一次性文件，避免测试代码继续影响正式环境。

## 三、Skill 持久化、加载与自动绑定

本周重点完善了 Skill 的创建、发现、索引和绑定流程。项目 Skill 统一保存在 `my_agent_next/skills/<skill-name>/` 下，以 `SKILL.md` 作为内容源，不再依赖用户目录下的 Codex Skill。对早期移植内容中残留的 Codex 描述进行了清理，统一改为面向“本应用”的说明，降低对特定产品名称和运行环境的耦合。

新增了轻量持久化索引 `skills/index.json`。索引保存 Skill 目录、名称、description 和 SHA-256 内容指纹，启动和发现 Skill 时会比较源文件指纹，手动修改 `SKILL.md` 后能够自动重建索引。Skill 正文仍按需加载，不会把所有 Skill 全部塞入模型上下文；Agent 的 Skill 绑定列表继续作为授权边界。

修复了 Agent 在对话中创建 Skill 后只在当前轮可用、退出后未自动绑定的问题。现在新建 Skill 会保存到项目 Skill 目录、更新索引，并自动绑定到发起创建的 Agent。通过创建句尾带“喵”的测试 Skill，验证了生成、持久化、绑定和下一轮加载流程。

后续又发现普通 Agent 对话与工作流 Agent 调用的上下文装配不一致：单独聊天会读取持久化 Skill，而工作流 Worker 最初只注入名称和人设。本周将人设、Skill 目录、路由结果、`SKILL.md` 正文、脚本资源和用户记忆提取为共享上下文装配逻辑，使两种调用方式保持一致。以小丑绑定的 `address-lookup` 为例，真实工作流事件已经能够看到 Skill 加载、`run_bash` 调用、输入参数和脚本输出。

## 四、Skill 市场与兼容性扫描

修复了 SkillsMP、ClawHub 中部分 Skill 预览失败和安装失败的问题，增强了对不同仓库目录结构、ZIP 顶层目录、`README.md` 与 `SKILL.md` 命名差异的兼容处理。安装后的 Skill 会进入项目目录并参与持久化索引，不再直接引用 `.codex` 中的系统 Skill。

新增 Skill 兼容性扫描层，在安装时和用户手动触发重新扫描时检查 frontmatter、平台要求、命令依赖、路径写法和可能的产品绑定痕迹。已安装 Skill 以绿色、黄色和红色展示兼容程度，并记录分数、问题、内容指纹和扫描时间。针对最初规则过严导致多个 Skill 被误判为黄色的问题，调整了判定权重，使颜色更接近实际可运行程度。

同时优化了已安装 Skill 的折叠体验，在“已安装”标题旁增加统一折叠按钮，保留原有卡片排版，方便用户收起本地内容后浏览市场。

## 五、Agent 对话、流式输出与中断控制

修复了工作台切换到 API 配置或市场后，返回对话页面看不到 Agent 中间回答的问题。现在页面隐藏期间仍保留正在进行的 SSE 请求和已有 DOM，重新进入工作台后可以继续看到流式输出、工具调用和最终回答。

完善了 Agent 工具调用展示，将工具名称、Skill、参数摘要和执行结果放入可折叠的运行记录。用户可以选择手动或自动权限模式，危险命令继续受权限规则限制。普通 Agent 对话新增“停止”按钮，能够取消当前模型流和工具循环，避免长时间生成或工具循环无法及时结束。

用户记忆、Agent 配置、对话线程和历史消息继续通过 SQLite 持久化。页面顶部的“选择 Agent”改为“选择对话”，同一个入口可以按分组选择 Agent 或工作流，并保留各自的会话状态。

## 六、代码优先的 LangGraph 工作流

本周完成了工作流功能的第一版完整闭环。工作流采用代码优先方式，用户直接在前端输入 LangGraph Python 源码，后端保存为草稿并进行 AST 静态验证。静态验证只检查入口、导入、危险调用和模块副作用，不会把代码解析成另一套自定义节点格式，也不会在 Web 进程中直接执行。

工作流规定无参数同步入口 `build_workflow()`，界面输入统一为 `{"message": "..."}`，最终输出统一为 `{"answer": "..."}`。Agent、Tool、Skill 和子工作流能力通过 `WorkflowRuntime` 注入。用户代码保存后按照 SHA-256 生成不可变 Python 运行产物，由独立 Worker 直接 import，并调用原生 LangGraph 的 `astream()` 执行。因此 `add_conditional_edges()`、条件选择、回边循环以及节点内部的 Python `if/for/while` 都是原生行为，不需要应用重新解释。

运行层增加了多重保护：LangGraph `recursion_limit`、总运行超时、Runtime 软取消以及 Worker 强制终止。测试中构造了具有条件边和回边的工作流，循环三次后正确返回；同时构造了节点内部 `while True` 的非协作代码，确认用户点击停止后能够强制终止独立进程。

## 七、工作流事件、嵌套调用与会话持久化

工作流执行过程中会通过 SSE 输出结构化事件，包括工作流开始、节点开始、节点输出、Agent 输入输出、Agent 工具调用、Skill 输入输出、子工作流进入返回、最终输出、取消、超时和错误。事件包含 `run_id`、父运行 ID、顺序号和时间戳，嵌套工作流可以按父子关系展示完整调用树。日志会截断超长内容，并对 API Key、Token、Cookie、密码等敏感字段进行脱敏。

前端将最终回答放在上方，完整运行过程放在回答下方。执行期间运行过程保持展开，收到最终回答后自动折叠为“运行过程（N 条）”，用户需要时可以重新展开查看每个节点和 Skill 的实际输入输出。

工作流会话也接入了现有 SQLite 对话表。选择工作流后可以新建、打开和删除对话，用户输入与最终回答写入 `chat_messages`，刷新页面后仍能恢复。工作流线程使用 `workflow:<workflow-id>` 作为归属，避免与 Agent ID 冲突。

## 八、两个实际工作流案例

为了验证运行链路，编写并保存了两个真实案例。

第一个是“诺登天气查询”工作流：用户输入先交给梅贝尔，梅贝尔以 JSON 形式填写城市参数；随后独立执行 `weather-skill`；最后将原问题、城市和真实天气结果交给诺登生成回答。天气 Skill 通过 `execution.yaml` 规定输入参数、Windows PowerShell 入口、Linux/macOS Bash 入口和超时时间，实现了跨平台独立执行。

第二个是“小丑天气嵌套工作流”：小丑先处理用户输入，如果没有明确地点，则实际加载并执行绑定的 `address-lookup` Skill，根据公网 IP 获取城市；随后通过依赖键 `weather_query` 嵌套调用完整的诺登天气工作流；最后再由小丑根据子工作流结果生成最终回答。该案例验证了 Agent Skill 持久化加载、工具调用、Skill 输出、父子工作流事件和嵌套输出返回。

真实运行中也确认了 IP 定位的边界：地址 Skill 查询到的是应用网络出口 IP，例如 Azure 日本节点，而不一定是用户物理位置。因此当前版本继续将 IP 定位作为近似方案，并在回答中明确提示代理或云出口可能造成误差。

## 九、STA 基础知识系统复习

除 Agent 项目开发外，本周继续对 STA 基础知识进行系统复习。重新梳理了 delay、slew、load 和 skew 的区别，并结合寄存器到寄存器路径推导 setup/hold 的分析方向。setup 关注最大路径延迟，防止数据来晚；hold 关注最小路径延迟，防止新数据来早。时钟到达时间变化会对 setup 和 hold 产生相反影响，数据路径插入 buffer 修复 hold 时也会消耗 setup slack。

复习了 PVT Corner、多场景时序分析和温度反转，明确 TT 角通过不能代表设计可以签核。进一步整理了同步与异步时钟域、generated clock、PLL/DLL/divider 的关系，以及 `set_clock_groups -asynchronous` 只定义 STA 分析边界、不能证明 CDC 结构安全的原因。

结合 Liberty 和 NLDM，复习了 cell rise/fall、transition、pin capacitance、timing arc 和 constraint arc，并完成二维查表、线性插值和双线性插值练习。理解了 output slew 会逐级成为下一级 input slew，因此整条路径需要逐级传播计算。

同时复习了 recovery/removal、异步复位同步释放、互连 RC 和 SPEF。明确布局布线后真实金属线、过孔、分支、对地电容和耦合电容会形成 net delay；SPEF 需要保留寄生参数的空间分布，不能只记录总 R 和总 C。

## 本周总结

本周完成了 `My Agent Next` 从功能验证到稳定版本的关键推进。项目已经形成“模型配置—Agent—Skill—工具—记忆—对话—工作流”的完整链路：Skill 可以创建、安装、扫描、索引和自动绑定；Agent 对话支持流式输出、工具轨迹和人工停止；用户编写的 LangGraph 代码能够作为原生管道在独立 Worker 中直接运行，并支持条件、循环、嵌套、Skill 调用、事件输出和强制中断；工作流对话和最终回答也已经持久化。

本周的主要收获不仅是增加功能，也是在不断暴露问题后逐步明确系统边界：草稿源码与运行产物分离，静态检查与执行隔离，Skill 索引与正文按需加载，Agent 绑定作为授权边界，工作流事件与最终回答分开展示，IP 定位结果与用户真实位置明确区分。经过完整回归和真实案例验证，当前版本可以作为后续继续扩展的稳定基线。

下一阶段将重点考虑低权限用户或容器级 Worker 隔离、工作流版本发布与回滚、运行轨迹持久化、可视化编辑简化，以及更可靠的用户位置输入方案。同时继续深入 SPEF 文件结构、RC 网络延迟计算和时序报告中的 cell/net delay 分析，将理论学习结果应用到实际 STA 约束和报告排查中。
